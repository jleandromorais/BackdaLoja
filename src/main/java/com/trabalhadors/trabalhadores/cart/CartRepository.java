package com.trabalhadors.trabalhadores.cart;

import com.trabalhadors.trabalhadores.Registro.Registro;
import org.springframework.data.jpa.repository.JpaRepository;


import java.util.List;
import java.util.Optional;

/*public interface CartRepository extends JpaRepository<Cart, Long> {
    Optional<Cart> findByUserIdAndStatus(Long userId, String status);

     // Método para encontrar o carrinho por usuário

    List<Cart> findByRegisterId(Long registerId);
    Optional<Cart> findByRegister_IdAndStatus(Long registroId, String status);


}
*/