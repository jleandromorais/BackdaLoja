package com.trabalhadors.trabalhadores.cart;

import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;
import java.util.Optional;

/*public interface CartItemRepository extends JpaRepository<CartItem, Long> {
    List<CartItem> findByCart(Cart cart); // Buscar itens de um carrinho específico
    List<Cart> findByRegisterId(Long id);
    List<CartItem> findByUserId(Long userId);
    List<CartItem> findByCartAndProduct(Cart cart, Product product);

}*/